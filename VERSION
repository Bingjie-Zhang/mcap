mcap-analysis-bundle v3.7.0 (2026-08-21): triple test gate — the 9-scenario golden dataset (synthetic, luna6-interface-faithful) ships in cases/training/ and runs as gate #3 inside run_tests (67 checks + 40 adversarial); eval_training.py is portable (bundled dataset by default, external path arg). Registry topic_aliases aligned to real luna6 topic names from the dataset.
