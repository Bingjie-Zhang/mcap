mcap-analysis-bundle v3.6.1 (2026-08-20): delivery reverted to tar bundles (install.sh copies again; git repo is developer backup only, not a user channel). Thin-shell architecture retained — agent TOMLs stay ~2KB and stable, so upgrading is overwrite-install without removing old agents. 66 checks.
